class Suns {
    constructor() {
        this.x = d;
        this.y = 0;

    }

    show() {
        image(sunImg, this.x, this.y, 1.225 * this.x, 1.225 * this.x);
    }

    show2() {
        image(sun_coolImg, 5.355 * this.x, this.y, 1.225 * this.x, 1.225 * this.x);
    }

    show3() {
        image(sun_surprise, 9.355 * this.x, this.y, 1.225 * this.x, 1.225 * this.x);
    }
}